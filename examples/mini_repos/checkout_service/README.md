# Checkout service

The checkout entry point lives in checkout.py.
Product data is kept in catalog.py.
Price calculations are kept in a separate pricing module.
A checkout total must retain two decimal places.
