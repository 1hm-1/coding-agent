# Order pipeline

The public entry point is render_report.
Input labels may contain surrounding whitespace.
The output format uses a dollar sign and two decimal places.
Normalization and formatting are separate modules.
