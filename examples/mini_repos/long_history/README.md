# Audit history fixture

This small repository contains a deliberately long but deterministic audit history.
The task is read-only: inspect the history and preserve the required facts.
The fixture exists to exercise context high-water and compression behavior.
